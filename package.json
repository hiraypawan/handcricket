{
  "name": "handcricket",
  "version": "2.9.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name handcricket",
    "test": "node tools/smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "latest",
    "jsdom": "^30.0.1"
  }
}
